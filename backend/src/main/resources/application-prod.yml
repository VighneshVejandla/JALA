#  D:\JALA\backend\src\main\resources\application-prod.yml

spring:

  datasource:
    url: ${SUPABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: false

  flyway:
    enabled: true

logging:
  level:
    root: INFO